# Doctrine Configuration
doctrine:
    dbal:
        dbname: "su_activity_log_test"

sulu_activity_log:
    storage:
        service_id: "sulu_activity_log.array_activity_log_storage"

services:
    sulu_activity_log.array_activity_log_storage:
        class: "Sulu\Component\ActivityLog\Storage\ArrayStorage\ArrayActivityLogStorage"
